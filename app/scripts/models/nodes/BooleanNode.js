/**
 * Created by Masha on 12/1/2014.
 */
define(['Node'], function(Node) {

    return Node.extend({
        updateValue: function () {
            //Doesn't need to do anything for 'Boolean' type node
        }
    });
});